using System;
using System.IO;

namespace Magistr.New.ThingTypes
{
    public class Package
    {
        public Package Open(string path)
        {
            return new Package();
        }
    }
}